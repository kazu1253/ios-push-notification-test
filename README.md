# ios-push-notification-test
We will create a test environment using GitHub Pages to establish a setup where a company system can function as a web app and receive native notifications without being published on app stores such as iOS App Store.
